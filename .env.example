DATABASE_URL=postgresql://user:pass@host:port/dbname
# Email Configuration for ARP Construction
# Copy this file and rename to .env, then fill in your actual credentials

# SMTP Configuration for Hostinger
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_USER=info@arpconstructionpro.org
SMTP_PASS=your_actual_email_password_here

# Notes:
# - SMTP_USER must be your full email address
# - SMTP_PASS is your email account password (not Hostinger control panel password)
# - Port 465 uses SSL/TLS (secure=true)
# - If port 465 doesn't work, try port 587 (uses STARTTLS)# Admin Portal Credentials
ADMIN_USER=Admin
ADMIN_PASS=RicardoPecco
SESSION_SECRET=a_very_secret_string_change_me
# - Never commit the .env file to git (it's already in .gitignore)
